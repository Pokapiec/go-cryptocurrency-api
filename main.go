package main

import (
	"fmt"
	// "github.com/gin-gonic/gin"
	// "net/http"
)

func main() {

	// 1. Download colly or sth else with scraping capabilities
	// 2. Scrape cryptocurrencies
	// 3. Add multiple routes that gets currencies
	// 4. Prepare html template to show currencies live
	// 5. Add saving hist prices to db

	fmt.Println("Hey")
	// router := gin.Default()
	// router.GET("/json", func(c *gin.Context) {
	// 	c.JSON(200, gin.H{
	// 		"first": "Pawecio",
	// 		"kurwa": "json wrocil!",
	// 	})
	// })
	// router.Run()

	// testList := []int{}

	// for i := 0; i < 10; i++ {
	// 	testList = append(testList, i)
	// }

	// fmt.Println(testList)

	// database.DbPrinter()

	// GetAllPrices()
	GetAllPrices()

}
